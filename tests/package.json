{
  "name": "obsidian-slides-plus-tests",
  "version": "0.1.0",
  "description": "Test suite for obsidian-slides-plus plugin",
  "main": "index.js",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "@vitest/coverage-v8": "^2.0.0",
    "typescript": "^5.3.0"
  }
}
